'use client';

import React from 'react';

export default function ContactForm({ onSubmit, onChange, name, email, message }) {
  return (
    <form onSubmit={onSubmit}>
      {/* Your form fields here */}
    </form>
  );
}